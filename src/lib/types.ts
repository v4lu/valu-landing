export type Post = {
	slug: string;
	title: string;
	date: string;
	desc: string;
	coverImage: string;
	path: string;
};

export type Metadata = {
	title: string;
	date: string;
	desc: string;
	cover: string;
};
